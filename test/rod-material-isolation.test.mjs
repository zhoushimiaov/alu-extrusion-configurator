import test from 'node:test';import assert from 'node:assert/strict';import {buildRodRack} from '../src/core/buildRodRack.js';
test('rod rack reveal children own isolated materials so product switching cannot hide siblings',()=>{const b=buildRodRack({width:.8,height:1.2,backPanel:'poster',shelf:'deck',casters:true,color:'chrome'});const meshes=b.group.children.filter(x=>x.isMesh);assert.ok(meshes.length>8);for(let i=0;i<meshes.length;i++)for(let j=i+1;j<meshes.length;j++){const a=Array.isArray(meshes[i].material)?meshes[i].material:[meshes[i].material],c=Array.isArray(meshes[j].material)?meshes[j].material:[meshes[j].material];for(const x of a)for(const y of c)assert.notEqual(x,y);}b.dispose();});
